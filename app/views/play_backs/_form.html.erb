<%= form_with(model: play_back, local: true) do |form| %>
  <% if play_back.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(play_back.errors.count, "error") %> prohibited this play_back from being saved:</h2>

      <ul>
        <% play_back.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :views %>
    <%= form.number_field :views %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
